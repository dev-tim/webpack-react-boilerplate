import { FETCH_JOB_LIST } from '../constants'

function searchRequestsReducer(state = [], action) {
  switch (action.type) {
    case 'Change me':
      return {};
    default:
      return state
  }
}

export default searchRequestsReducer;